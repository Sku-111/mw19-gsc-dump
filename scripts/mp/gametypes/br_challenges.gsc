// IW8 GSC SOURCE
// Generated by https://github.com/xensik/gsc-tool

init()
{
    if ( scripts\mp\utility\game::getgametype() != "br" )
        return;

    level.br_challengeevaluatorfunc = ::getallspawninstances;
    _id_12B14( "br_mastery_fiveContracts", ::player_equipment_init );
    _id_12B14( "br_mastery_pointBlank_airstrike", ::_id_127DB );
    _id_12B14( "br_mastery_pointBlank_tomahawk", ::_id_127DC );
    _id_12B14( "br_mastery_c4VehicleMultKill", ::force_dismount );
    _id_12B14( "br_mastery_ghostRideWhip", ::scavenger_vo_when_close );
    _id_12B14( "br_mastery_roundKillExecute", ::_id_12DB8 );
    _id_12B14( "br_mastery_travelogue", ::_id_13D08 );
}

_id_12B14( var_0, var_1 )
{
    if ( !isdefined( level.debugprintteams ) )
        level.debugprintteams = [];

    level.debugprintteams[var_0] = var_1;
}

getallspawninstances( var_0, var_1 )
{
    if ( !isdefined( level.debugprintteams ) )
        return;

    if ( istrue( level.getarenapickupattachmentoverrides ) )
        return;

    var_2 = level.debugprintteams[var_0];

    if ( isdefined( var_2 ) )
        self thread [[ var_2 ]]( var_0, var_1 );
}

cheesewedgeprompt( var_0 )
{
    if ( getdvarint( "scr_br_challenge_debug", 0 ) )
        iprintlnbold( "Mastery Challenge: " + var_0 + " completed!" );

    scripts\cp\vehicles\vehicle_compass_cp::_id_12004( var_0 );
}

_id_11B1D( var_0 )
{
    if ( !isdefined( level.teamdata[var_0] ) )
        return;

    foreach ( var_2 in level.teamdata[var_0]["players"] )
        _id_13D07( var_2 );

    if ( getdvarint( "scr_br_core_trios_or_quads", 0 ) )
    {
        _id_13F74( var_0 );
        _id_11E54( var_0 );
    }
}

_id_13F74( var_0 )
{
    if ( !isdefined( var_0 ) )
        return;

    var_1 = 0;
    var_2 = 0;
    var_3 = max( 1, level.maxteamsize );

    foreach ( var_5 in level.teamdata[var_0]["players"] )
    {
        var_1 = var_1 + var_5.deaths;

        if ( isdefined( var_5._id_11BE1 ) )
            var_1 = var_1 + var_5._id_11BE1;

        if ( scripts\mp\utility\player::isreallyalive( var_5 ) )
            var_2++;
    }

    if ( var_1 == 0 )
    {
        foreach ( var_5 in level.teamdata[var_0]["players"] )
            var_5 cheesewedgeprompt( "br_mastery_untouchable" );
    }
}

_id_11E53()
{
    if ( !isdefined( self ) || !isdefined( self.team ) )
        return;

    var_0 = self.deaths;
    var_1 = self.team;

    foreach ( var_3 in level.teamdata[var_1]["players"] )
    {
        if ( scripts\mp\utility\player::isreallyalive( var_3 ) )
        {
            if ( !isdefined( var_3._id_11BE1 ) )
                var_3._id_11BE1 = 0;

            var_3._id_11BE1 = var_3._id_11BE1 + var_0;
            break;
        }
    }
}

_id_11E54( var_0 )
{
    var_1 = 7;

    if ( !isdefined( var_0 ) )
        return;

    if ( scripts\mp\gametypes\br.gsc::usingtacmap() )
        return;

    var_2 = 0;

    foreach ( var_4 in level.teamdata[var_0]["players"] )
    {
        var_2 = var_2 + var_4.deaths;

        if ( isdefined( var_4._id_11BE1 ) )
            var_2 = var_2 + var_4._id_11BE1;
    }

    if ( var_2 >= var_1 )
    {
        foreach ( var_4 in level.teamdata[var_0]["players"] )
            var_4 cheesewedgeprompt( "br_mastery_neverSayDie" );
    }
}

player_equipment_init( var_0, var_1 )
{
    if ( !isdefined( self.team ) )
        return;

    if ( scripts\mp\gametypes\br.gsc::get_int_or_0( self.egress_landlord_vo ) < 5 )
        return;

    var_2 = isdefined( level.gulag ) && !istrue( level.gulag.shutdown );

    foreach ( var_4 in level.teamdata[self.team]["players"] )
    {
        if ( !scripts\mp\utility\player::isreallyalive( var_4 ) )
            return;

        if ( var_2 && var_4 scripts\mp\gametypes\br_public.gsc::updateinstantclassswapallowedinternal() )
            return;
    }

    cheesewedgeprompt( var_0 );
}

_id_127DB( var_0, var_1 )
{
    var_2 = var_1._id_123A1;
    var_3 = var_1._id_13A8A;

    if ( !isdefined( var_2 ) || !isdefined( var_2.brbonusxpallowed ) || !isdefined( var_2.streakinfo ) || !isdefined( var_3 ) )
        return;

    var_4 = var_2.streakinfo;

    if ( distancesquared( self.origin, var_3 ) > squared( 432 ) )
        return;

    self endon( "death_or_disconnect" );
    self notify( "pointBlank_airstrike_killtracker" );
    self endon( "pointBlank_airstrike_killtracker" );
    scripts\engine\utility::_id_143B9( 10.0, "airstrike_finished_" + var_2.brbonusxpallowed );

    if ( !scripts\mp\utility\player::isreallyalive( self ) )
        return;

    if ( var_4.kills >= 3 )
        cheesewedgeprompt( "br_mastery_pointBlankStreakKill" );
}

_id_127DC( var_0, var_1 )
{
    var_2 = var_1.streakinfo;
    var_3 = var_1._id_13A8A;

    if ( !isdefined( var_2 ) )
        return;

    if ( !isdefined( var_3 ) )
        return;

    if ( distancesquared( self.origin, var_3 ) > squared( 432 ) )
        return;

    self endon( "death_or_disconnect" );
    self notify( "pointBlank_tomahawk_killtracker" );
    self endon( "pointBlank_tomahawk_killtracker" );
    scripts\engine\utility::_id_143BA( 20, "cluster_strike_finished" );

    if ( !scripts\mp\utility\player::isreallyalive( self ) )
        return;

    if ( var_2.kills >= 3 )
        cheesewedgeprompt( "br_mastery_pointBlankStreakKill" );

    return;
}

force_dismount( var_0, var_1 )
{
    var_2 = var_1.meansofdeath;
    var_3 = var_1.inflictor;

    if ( !isdefined( var_2 ) || var_2 != "MOD_EXPLOSIVE" )
        return;

    if ( !vandalize_attack_nodes( var_3 ) )
        return;

    self endon( "disconnect" );
    level endon( "game_ended" );
    self notify( "updateC4VehicleMultKill" );
    self endon( "updateC4VehicleMultKill" );

    if ( !isdefined( self._id_12A83 ) )
        self._id_12A83 = 0;

    self._id_12A83++;
    wait 4.0;

    if ( isdefined( self._id_12A83 ) && self._id_12A83 >= 3 )
    {
        cheesewedgeprompt( var_0 );
        var_4 = force_call_lz( var_3 );
        var_5 = isdefined( var_4 ) && isdefined( var_4.team ) && isdefined( self.team ) && var_4.team == self.team;

        if ( var_5 )
            var_4 cheesewedgeprompt( var_0 );
    }

    self._id_12A83 = undefined;
}

vandalize_attack_nodes( var_0 )
{
    if ( !isdefined( var_0 ) )
        return 0;

    if ( isdefined( var_0.vehiclename ) && !var_0 scripts\common\vehicle_code::vehicle_is_stopped() )
    {
        var_1 = var_0 getlinkedchildren();

        foreach ( var_3 in var_1 )
        {
            if ( isdefined( var_3.weapon_name ) && var_3.weapon_name == "c4_mp_p" )
                return 1;
        }
    }
    else if ( isdefined( var_0.weapon_name ) && var_0.weapon_name == "c4_mp_p" )
    {
        var_5 = var_0 getlinkedparent();

        if ( isdefined( var_5 ) && isdefined( var_5.vehiclename ) && !var_5 scripts\common\vehicle_code::vehicle_is_stopped() )
            return 1;
    }

    return 0;
}

force_call_lz( var_0 )
{
    if ( isdefined( var_0 ) && isdefined( var_0.weapon_name ) && var_0.weapon_name == "c4_mp_p" )
    {
        var_1 = var_0 getlinkedparent();

        if ( isdefined( var_1 ) )
            return var_1.owner;
    }

    return undefined;
}

_id_12DB8( var_0, var_1 )
{
    var_2 = var_1.player;

    if ( !isdefined( var_2 ) )
        return;

    if ( isdefined( var_2.modifiers["execution"] ) && var_2.modifiers["execution"] == 1 )
        var_2 cheesewedgeprompt( "br_mastery_roundKillExecute" );
}

scavenger_vo_when_close( var_0, var_1 )
{
    self endon( "disconnect" );
    level endon( "game_ended" );

    if ( !isdefined( self ) )
        return;

    var_2 = var_1.onplayerkillednew;
    var_3 = var_1._id_11A6C;

    if ( istrue( var_2 ) )
    {
        if ( istrue( self.should_run_sp_stealth ) && self._id_12A84 != 0 )
            return;

        self.should_run_sp_stealth = 1;
    }

    if ( !istrue( self.should_run_sp_stealth ) )
        return;

    self notify( "ghostRideWhip" );
    self endon( "ghostRideWhip" );

    if ( !isdefined( self._id_12A84 ) )
        self._id_12A84 = 0;

    if ( istrue( var_3 ) )
        self._id_12A84++;

    wait 4.0;

    if ( !isdefined( self ) )
        return;

    if ( isdefined( self._id_12A84 ) && self._id_12A84 >= 3 )
        cheesewedgeprompt( var_0 );

    self._id_12A84 = undefined;
    self.should_run_sp_stealth = undefined;
}

_id_13D08( var_0, var_1 )
{
    var_2 = -1;

    if ( getdvarint( "scr_br_core_trios_or_quads", 0 ) == 0 )
        return;

    var_3 = var_1.inflictor;

    if ( !isdefined( var_3 ) )
        return;

    if ( !isplayer( var_3 ) && isdefined( var_3.owner ) && isplayer( var_3.owner ) )
        var_3 = var_3.owner;
    else
        return;

    var_4 = var_1.victim;

    if ( !isdefined( var_4 ) || !isplayer( var_4 ) )
        return;

    var_5 = 0;
    var_6 = var_3 scripts\mp\gametypes\br_callouts.gsc::removematchingents_bymodel( var_3 );

    if ( var_6 != var_2 )
        var_5 = var_5 | 1 << var_6;

    var_6 = var_4 scripts\mp\gametypes\br_callouts.gsc::removematchingents_bymodel( var_4 );

    if ( var_6 != var_2 )
        var_5 = var_5 | 1 << var_6;

    if ( var_5 == 0 )
        return;

    foreach ( var_8 in level.teamdata[var_3.team]["players"] )
    {
        if ( !isdefined( var_8._id_14727 ) )
            var_8._id_14727 = 0;

        var_8._id_14727 = var_8._id_14727 | var_5;
    }
}

_id_13D07( var_0 )
{
    var_1 = 12;

    if ( !isdefined( var_0._id_14727 ) || var_0._id_14727 == 0 )
        return;

    var_2 = 0;

    for ( var_3 = 0; var_3 < level.calloutglobals._id_11E29.size; var_3++ )
    {
        var_4 = var_0._id_14727 & 1 << var_3;

        if ( var_4 )
            var_2++;
    }

    if ( var_2 >= var_1 )
        var_0 cheesewedgeprompt( "br_mastery_travelogue" );

    return;
}
